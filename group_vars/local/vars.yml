---
# Variabili per provisioning manuale su VM locale
# (Packer usa le proprie variabili definite in packer/variables.pkr.hcl)

ansible_user: vapt
ansible_ssh_private_key_file: ~/.ssh/vapt-vm

vm_user: vapt
